# In local development over HTTPS, keep empty to route API calls through the Vite HTTPS proxy to FastAPI.
# In production, set to your custom backend domain (e.g., https://api.voxguard.ai).
VITE_API_BASE_URL=
